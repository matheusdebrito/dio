"use strict";
function addNumber(x, y) {
    return x + y;
}
function addToHello(name) {
    return `Hello ${name}`;
}
let soma = addNumber(4, 2);
console.log(addToHello("Matheus"));
console.log(soma);
